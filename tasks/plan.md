# Operations console preview plan

1. Add the information architecture and design tokens without changing backend behavior.
2. Render a live overview and an honest 32-slot map from existing modem data.
3. Add conversation, maintenance, alert, report, and audit preview views.
4. Verify responsive behavior and accessibility in a real browser.
5. Present the local preview for approval before any persistence or scheduler work.

Risks: live data currently has no physical slot number or MSISDN; those fields must remain explicitly unassigned. Automated maintenance is billable and therefore stays disabled during review.

